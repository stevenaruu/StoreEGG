export interface IGridCardProps {
    id: number;
    title: string;
    description: string;
    price: number;
    image: string;
    transaction: string;
}

export interface IGridCardState {
    
}