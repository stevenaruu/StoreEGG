export interface IMinigameSectionProps {
    title: string;
    textColor: string;
    visibility: string;
    message: string;
    egg: string;
    prize:string;
    handleClick: () => void;
}

export interface IMinigameSectionState {
    
}